{
  "name": "@ara/api",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "lint": "ruff check app tests",
    "typecheck": "echo \"Python typecheck: configure mypy when ready\"",
    "test": "pytest"
  }
}
